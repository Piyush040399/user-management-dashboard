# React  + Typescript + Tailwind css Techstack used a User Management Dashboard.

# Steps to install

# Clone the repo.
# Do npm install
# create a .env file in root folder and copy the .env.example to use MOCK API url.
# and do npm run dev to run the project in local